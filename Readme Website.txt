This website uses:
HTML5
CSS3
PHP
JavaScript
mySQL Database

Home page is as shown and navigation bar works as mentioned in problem.

The Bikes availability is shown based on the number of bikes of that model in database

Add bikes page can be used to add more bikes to the databse or can be removed using negative numbers.

The page currently shows only 6 bikes at a time and their availability in Bikes page becuase dynamic view according to number of bikes in databse will require AJAX.